{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Docusaurus",
      "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/docusaurus",
      "args": ["start", "--no-open"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
